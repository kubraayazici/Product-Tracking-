apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data-producttracking-db-0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: hostpath
  volumeName: postgres-pv